import React from 'react';

const UserMenuUsername = ({username}) => {
    return (
        <div className="usermenu-username">
            @{username}
        </div>
    );
};

export default UserMenuUsername;